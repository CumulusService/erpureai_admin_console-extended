<!DOCTYPE html>
<html>
<head>
    <title>Permission Test</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Microsoft Graph Permission Test</h1>
            <a href="/Account/SignOut" class="btn btn-outline-secondary">Sign Out</a>
        </div>

        <div class="alert alert-info">
            <strong>Test:</strong> This will check what Microsoft Graph permissions we have.
        </div>

        <div class="card">
            <div class="card-body">
                <h5>Test Graph API Permissions</h5>
                
                <form method="post">
                    <button type="submit" class="btn btn-primary">Run Permission Tests</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>