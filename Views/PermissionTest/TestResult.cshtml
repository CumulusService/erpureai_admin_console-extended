<!DOCTYPE html>
<html>
<head>
    <title>Permission Test Results</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Permission Test Results</h1>
            <a href="/Account/SignOut" class="btn btn-outline-secondary">Sign Out</a>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">
                <h4>❌ Error</h4>
                <p>@ViewBag.Error</p>
            </div>
        }

        <div class="card">
            <div class="card-body">
                <h5>Permission Test Results:</h5>
                
                <div class="row">
                    <div class="col-12 mb-2">
                        <strong>Application Access:</strong> @ViewBag.Test1
                    </div>
                    <div class="col-12 mb-2">
                        <strong>User Access:</strong> @ViewBag.Test2
                    </div>
                    <div class="col-12 mb-2">
                        <strong>Group Access:</strong> @ViewBag.Test3
                    </div>
                </div>
            </div>
        </div>

        <div class="card mt-4">
            <div class="card-body">
                <h5>Required Permissions for B2B Invitations:</h5>
                <ul>
                    <li><code>User.Invite.All</code> - Send guest user invitations</li>
                    <li><code>User.ReadWrite.All</code> - Read and write all users</li>
                    <li><code>Directory.ReadWrite.All</code> - Read and write directory data</li>
                    <li><code>Group.ReadWrite.All</code> - Read and write all groups</li>
                </ul>
                <p><strong>Important:</strong> After adding permissions, click "Grant admin consent"!</p>
            </div>
        </div>

        <div class="mt-3">
            <a href="/PermissionTest/Test" class="btn btn-primary">Test Again</a>
            <a href="/SimpleTest/Test" class="btn btn-secondary">Try B2B Invitation</a>
        </div>
    </div>
</body>
</html>